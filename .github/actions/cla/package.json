{
  "name": "vyos-cla-action",
  "version": "1.0.0",
  "private": true,
  "description": "First-party CLA check/sign action for the VyOS fleet",
  "main": "src/main.js",
  "scripts": {
    "test": "node --test",
    "build": "ncc build src/main.js -o dist --license licenses.txt"
  },
  "dependencies": {
    "@actions/core": "^1.11.1",
    "@actions/github": "^6.0.0"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.44.1"
  }
}
